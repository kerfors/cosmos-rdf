# Environment for the notebook pipeline. Not needed to consume the deliverables —
# scripts/ci_check.py, which is what CI runs, needs rdflib alone.
#
# linkml is pinned, and 20_generate.ipynb and 70_generate_qbc.ipynb assert the
# version before generating. Decision D6: this repo pins every generator option
# explicitly because the library's Python defaults differ from the CLI defaults,
# and those defaults are the 1.11.1 ones. Bumping is a deliberate action, checked
# by re-running 20_generate.ipynb, 30_validate.ipynb and 77_generate_qbc_shapes.ipynb.

linkml==1.11.1
rdflib>=7.0
pandas>=2.0
pyshacl>=0.26
pyyaml>=6.0
